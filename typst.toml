[package]
name = "wen"
version = "1.0.0"
compiler = "0.12.0"
entrypoint = "lib.typ"
authors = ["Tieming Geng <https://github.com/yopming>"]
license = "GPLv3"
description = "template for articles"

[template]
path = "template"
entrypoint = "main.typ"
